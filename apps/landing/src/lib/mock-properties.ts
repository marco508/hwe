/**
 * @deprecated
 * Les données fictives ne sont plus utilisées : la landing affiche uniquement
 * les biens réels stockés en base via l'API publique. Ce fichier est conservé
 * uniquement pour ne pas casser les imports historiques. Vous pouvez le
 * supprimer sans risque.
 */
import type { Property } from "@hwe/types";

export const MOCK_PROPERTIES: Property[] = [];
